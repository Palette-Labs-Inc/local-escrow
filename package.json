{
  "name": "@nosh/escrow_poc",
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*",
      "examples/*"
    ]
  },
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=22.1.5"
  },
  "scripts": {
    "lint": "biome check .",
    "format": "biome format . --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
